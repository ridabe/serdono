{
  "name": "@serdono/core",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "description": "Regras de negócio puras (Fit Score, validações, roteamento de IA) — SPEC.md §3, SDD-3.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "~5.9.2"
  }
}
