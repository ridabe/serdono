import { EbookLandingScreen } from "../../components/ebook/EbookLandingScreen";

// Landing pública da isca gratuita (SDD-139) — sem auth, fora de (protected).
export default function EbookLanding() {
  return <EbookLandingScreen />;
}
