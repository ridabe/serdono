import { EbookFormScreen } from "../../components/ebook/EbookFormScreen";

// Formulário de captura + download (SDD-139). `/ebook/baixar`.
export default function EbookBaixar() {
  return <EbookFormScreen />;
}
